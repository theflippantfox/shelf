/**
 * Border radius tokens — direct port from web and Flutter design guide.
 */

export const radii = {
  /** Badges, chips */
  sm: 6,
  /** Cards, inputs, buttons */
  md: 10,
  /** Bottom sheets, modals */
  lg: 14,
  /** Large sheets */
  xl: 20,
  /** Pill buttons/tabs */
  pill: 999,
  /** Circular (avatars, FAB) */
  full: 9999,
} as const;
